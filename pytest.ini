[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -p no:cacheprovider
markers =
    smoke: tests that exercise real local integrations kept outside the fast loop
